{
  "name": "pdf-test",
  "version": "1.0.0",
  "description": "Browser PDF generation test — Centre Alilham",
  "scripts": {
    "build": "browserify test.js -o bundle.js",
    "build:watch": "watchify test.js -o bundle.js --poll"
  },
  "dependencies": {
    "pdfkit": "0.13.0",
    "blob-stream": "^0.1.3",
    "iconv-lite": "^0.6.3"
  },
  "devDependencies": {
    "browserify": "^17.0.1",
    "brfs": "^2.0.2",
    "watchify": "^4.0.0"
  }
}
